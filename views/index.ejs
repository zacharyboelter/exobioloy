<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="/normalize.css">
    <link rel="stylesheet" href="/skeleton.css">
    <title>Scientist Index</title>
</head>

<body>
    <div class="container">
        <h1>Scientist</h1>

        <table>
            <% for(let i=0; i < scientists.length; i++) { %>
                <tr>

                    <td>

                        <%= scientists[i].name %>
                    </td>
                    <td>
                        <!-- <a href="/planets/<%= i %>"> -->
                            <%= scientists[i].specialty %>
                        </a>
                    </td>
                    <td>
                        <%= scientists[i].description %>
                    </td>
                </tr>
                <% } %>
        </table>
        <button><a href="/planets/new">Add New</a></button>
    </div>
</body>

</html>